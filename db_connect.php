<?php
$servername = "localhost";  // ที่อยู่เซิร์ฟเวอร์ฐานข้อมูล
$username = "root";         // ชื่อผู้ใช้ฐานข้อมูล
$password = "";             // รหัสผ่านฐานข้อมูล
$dbname = "market_booking"; // ชื่อฐานข้อมูลของคุณ

// สร้างการเชื่อมต่อ
$conn = new mysqli($servername, $username, $password, $dbname);

// ตรวจสอบการเชื่อมต่อ
if ($conn->connect_error) {
    // หากการเชื่อมต่อไม่สำเร็จ
    die("การเชื่อมต่อล้มเหลว: " . $conn->connect_error);
}

// สามารถเพิ่มการเชื่อมต่อที่เสร็จสิ้นแล้วที่นี่
// ตัวอย่างเช่น:
// echo "เชื่อมต่อฐานข้อมูลสำเร็จ";

// สามารถเพิ่มคำสั่ง SQL อื่นๆ ที่ต้องการใช้งานต่อไป
?>
